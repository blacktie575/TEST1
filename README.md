<!-- # Function Test README

This repository contains Python scripts that demonstrate and test various functions related to arithmetic, digit manipulation, and list operations.

## File Structure

- `numbers/simp.py`: Contains simple arithmetic functions.
- `numbers/comp.py`: Contains complex functions related to digits and list operations.
- `tools/col.py`: Implements a custom `myzip` function to zip two collections.
- `tools/test.py`: A script to test the functions from the above files interactively.

## Functions

### Simple Arithmetic Functions (`numbers/simp.py`)

#### `add_numbers(a, b)`
- Adds two numbers and returns the result.

#### `subtract_numbers(a, b)`
- Subtracts the second number from the first and returns the result.

### Complex Functions (`numbers/comp.py`)

#### `sum_of_digits(number)`
- Calculates the sum of digits in a given number.

#### `is_palindrome(number)`
- Checks if a given number is a palindrome.

### Custom List Function (`tools/col.py`)

#### `myzip(it1, it2)`
- Implements the `zip` function for two collections.

## How to Use

1. Navigate to the `tools` directory in your terminal.

2. Run the `test.py` script using the following command in terminal:
   py test.py -->
